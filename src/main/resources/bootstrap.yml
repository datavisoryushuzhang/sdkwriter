spring:
  application:
    name: sdkwriter
  cloud:
    config:
      server:
        git:
          uri: 'https://github.com/datavisoryushuzhang/sdkwriter'
          searchPaths: config
        bootstrap: true

encrypt:
  key: Datavisor_2019!